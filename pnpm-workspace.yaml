overrides:
  vite: npm:rolldown-vite@7.3.1

# pnpm 11 removed onlyBuiltDependencies in favour of allowBuilds, and defaults
# strictDepBuilds to true, so an unlisted package with a build script is a hard
# error rather than a warning.
allowBuilds:
  esbuild: true
